name: Deploy Backend to Render

on:
  push:
    branches:
      - main

env:
  DOCKER_IMAGE: adityaisme/mockapi-backend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6   # Updated

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4   # Updated

      - name: Log in to Docker Hub
        uses: docker/login-action@v4   # Updated
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v7   # Updated (if you use it)
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ${{ env.DOCKER_IMAGE }}:latest
            ${{ env.DOCKER_IMAGE }}:${{ github.sha }}

      - name: Trigger Render Deploy
        run: |
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}